# react-scripts@5 declares an optional peer of typescript ^3||^4, but modern
# three.js type declarations require typescript 5. Relax peer resolution so the
# demo installs cleanly with a current TypeScript.
legacy-peer-deps=true
